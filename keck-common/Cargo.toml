[package]
name = "keck-common"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
# Enable user-space only features (aya::Pod impls, std)
userspace = ["dep:aya"]

[dependencies]
aya = { version = "0.13", optional = true }
